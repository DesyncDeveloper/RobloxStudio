from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
from Modules.web_api import WebAPI
from Modules.GitHub import GitHubConnect

GitHub = GitHubConnect.Get()

class RobloxStudio:
    @staticmethod
    async def handle_set_data():
        print("YE man")

    @staticmethod  
    async def handle_get_data():
        ObjectPath = await WebAPI.GetArgs('ObjectPath', 'Unknown')
        ObjectName = await WebAPI.GetArgs('ObjectName', 'Unknown')

        if ObjectPath == "Unknown":
            return "Error: Object Path Unknown"

        if ObjectName == "Unknown":
            return "Error: Object Name Unknown"
        
        FilePath = f"src/{ObjectPath}/{ObjectName}"

        return GitHub.get_file_contents(FilePath)
